{
    "name": "api-suscripciones",
    "version": "1.0.0",
    "description": "API simulada para gestión de suscripciones usando json-server",
    "scripts": {
        "start": "node node_modules/json-server/lib/cli/bin.js --watch db.json --port $PORT --host 0.0.0.0",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "json-server": "^0.17.4"
    }
}
